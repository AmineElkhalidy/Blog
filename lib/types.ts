export interface SimpleBlog {
  title: string;
  smallDescription: string;
  currentSlug: string;
  image: any;
}

export interface BlogPost {
  currentSlug: string;
  title: string;
  image: any;
  content: any;
}
